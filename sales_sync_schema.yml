version: 2

models:
  - name: sales_sync
    columns:
      - name: org_id
        tests:
          - not_null
          - unique
        tags: ["primary-key"]
      - name: ORG_ID
      - name: usage
